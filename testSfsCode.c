/*
 * Name: testSfsCode
 * Description: Very intial code. Mostly pseudo-code.
 */

// IF (YOU ARE NOT A DEVELOPER) {READ ONLY THE COMMENTS;}
// THIS IS A COMMENT
/* THIS IS A COMMENT TOO*/




//Header files

#include <stdio.h>  //Std io
#incldue <time.h>   //For clock()
 r  
    //Add actuations to be done manually
    //Get input command
    //Actuation based on input
    while((c = getChar()) != 'x') {
        switch (c) {
            case 'a':
                //Do something
            //Fill out other cases
                
            default:
                break;
        }
    }
    
    
    //START OF TIMED SEQUENCE
    
    start_t = clock();
    
    while(1) {
        switch (clock()) {
            case 1:
                //Do whatever happens in 1 sec.
            case 2:
                //Do whatever happens in 2 sec.
            case 3:
                //Do whatever happens in 3 sec.
            case 4:
                //Do whatever happens in 4 sec.
            case 5:
                //Do whatever happens in 5 sec.
            case 6:
                //Do whatever happens in 6 sec.
            case 7:
                //Do whatever happens in 7 sec.
            case 8:
                //Do whatever happens in 8 sec.
            case 9:
                //Do whatever happens in 9 sec.
            case 10:
                //Do whatever happens in 10 sec.
                
            default:
                break;
        }
    }
    return 0;
}